export interface GlobalAddressType {
    label: string; //home | office | other
    addressLine: string;
    area: string;
    district: string;
    city: string;
    state: string;
    country: string;
    postalCode: string;
}